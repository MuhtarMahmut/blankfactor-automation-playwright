{
  "name": "blankfactor-automation-playwright",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "tests": "npx playwright test  --headed",
    "tests-headless": "npx playwright test",
    "test-tag": "npx playwright test --headed --grep @wip",
    "test-tag-headless": "npx playwright test --grep @wip",
    "test-smoke": "npx playwright test --grep @smoke",
    "report": "npx playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "@types/node": "^25.1.0",
    "dotenv": "^17.2.3"
  }
}
